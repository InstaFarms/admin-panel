/**
 * Layout-related constants (breakpoints, shell behavior).
 * Used by SidebarContext, Navbar, and other layout components.
 */

/** Min viewport width (px) treated as desktop; sidebar stays open above this. */
export const SIDEBAR_DESKTOP_BREAKPOINT_PX = 768;
