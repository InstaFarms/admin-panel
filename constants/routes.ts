/**
 * Route paths used in navigation, breadcrumbs, and redirects.
 * Use these instead of string literals when the path appears in more than one place.
 */

/** Base path for all admin routes. */
export const ADMIN_BASE_PATH = "/admin";

/** Main admin dashboard page. */
export const ADMIN_DASHBOARD_PATH = "/admin/dashboard";

/** Admins list and resource path (list, create, [id]). */
export const ADMIN_ADMINS_PATH = "/admin/admins";

/** Create-new-admin page. */
export const ADMIN_ADMINS_CREATE_PATH = "/admin/admins/create";

/** Users list and resource path (list, create, [id]). */
export const ADMIN_USERS_PATH = "/admin/users";

/** Property types list and resource path (list, create, [id]). */
export const ADMIN_PROPERTY_TYPES_PATH = "/admin/propertyTypes";
