// Bulk pre-loading is no longer needed — all location dropdowns use async server-side search.
// This file is kept as a stub so existing imports don't break during transition.
export type LocationArea = { id: string; area: string; slug?: string };
